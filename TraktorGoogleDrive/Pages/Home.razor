@page "/"
@inject NavigationManager Navigation

@code {
    // The collection view is the app; "/" is just an entry point.
    protected override void OnInitialized() => Navigation.NavigateTo("/music", replace: true);
}
